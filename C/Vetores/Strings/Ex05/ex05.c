/*Escreva um programa que leia uma palavra e verifique se ela é um palíndromo. Palíndromo é a palavra cuja leitura é a mesma, quer se faça da direita para a esquerda, 
quer da esquerda para a direita. Exemplo: ovo, anilina */
#include<stdio.h>
#include<string.h>
#include<ctype.h>

int main(){

    char palavra[47];

    printf("Digite uma palavra: ");
    gets(palavra);

    int i;

    for(i=0/ i<=strlen(palavra); i++){
        
    }
}
